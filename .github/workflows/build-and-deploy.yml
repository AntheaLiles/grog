name: Build and Deploy Org Site

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Julia
        uses: julia-actions/setup-julia@v2
        with:
          version: '1.9'
          
      - name: Cache Julia packages
        uses: julia-actions/cache@v2
        with:
          cache-name: julia-packages-${{ runner.os }}-${{ hashFiles('**/Project.toml') }}

      - name: Create new Project.toml
        run: |
          echo "name = 'OrgSiteGenerator'" > Project.toml
          echo "uuid = '5bbd6b1d-f185-427c-8e1a-4305d105cf5d'" >> Project.toml
          echo "version = '0.1.0'" >> Project.toml

      - name: Install dependencies
        run: julia -e 'using Pkg; Pkg.instantiate()'

      - name: Run tests
        run: julia -e 'using Pkg; Pkg.activate("."); Pkg.test()'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Node.js dependencies
        run: npm ci

      - name: Build site and copy files
        run: |
          julia scripts/build.jl
          npx vite build
          cp -R public/css public/src dist/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist
          name: github-pages

  deploy:
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        with:
          artifact_name: github-pages